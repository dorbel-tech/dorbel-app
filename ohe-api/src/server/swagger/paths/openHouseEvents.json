{
  "/ohe": {
    "post": {
      "summary": "Get open house event",
      "description": "Get a open house event to the system",
      "parameters": [
        {
          "name": "body",
          "in": "body",
          "schema": {
            "$ref": "#/definitions/OpenHouseEvent"
          }
        },
        {
          "name": "x-user-profile",
          "in": "header",
          "required": true,
          "type": "string",
          "description": "Stringified JSON object with the user's profile information"
        }
      ],
      "tags": [
        "oheController",
        "authenticate"
      ],
      "responses": {
        "201": {
          "description": "Open house event created succesfully",
          "schema": {
            "$ref": "#/definitions/OpenHouseEvent"
          }
        },
        "default": {
          "description": "Unexpected error",
          "schema": {
            "$ref": "#/definitions/Error"
          }
        }
      }
    }
  }
}