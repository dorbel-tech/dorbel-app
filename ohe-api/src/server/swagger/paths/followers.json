{
  "/event/follow": {
    "post": {
      "summary": "Enables a user to follow an open house event",
      "description": "Enables a user to follow an open house event in the system",
      "parameters": [
        {
          "name": "body",
          "in": "body",
          "schema": {
            "$ref": "#/definitions/AddFollowRequest"
          }
        },
        {
          "name": "x-user-profile",
          "in": "header",
          "required": true,
          "type": "string",
          "description": "Stringified JSON object with the user's profile information"
        }
      ],
      "tags": [
        "followersController",
        "authenticated"
      ],
      "responses": {
        "201": {
          "description": "Follower created succesfully",
          "schema": {
            "$ref": "#/definitions/Follower"
          }
        },
        "default": {
          "description": "Unexpected error",
          "schema": {
            "$ref": "#/definitions/Error"
          }
        }
      }
    }
  },
  "/event/follow/{id}": {
    "delete": {
      "summary": "Unfollows a user from an open house event",
      "description": "Unfollows a user from an open house event in the system",
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "type":"integer",
          "required": true
        },
        {
          "name": "x-user-profile",
          "in": "header",
          "required": true,
          "type": "string",
          "description": "Stringified JSON object with the user's profile information"
        }
      ],
      "tags": [
        "followersController",
        "authenticated"
      ],
      "responses": {
        "200": {
          "description": "Unfollow completed succesfully"
        },
        "default": {
          "description": "Unexpected error",
          "schema": {
            "$ref": "#/definitions/Error"
          }
        }
      }
    }
  }
}