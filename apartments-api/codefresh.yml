version: '1.0'

steps:
  build-the-image:
    type: build
    description: Builds apartments-api
    working-directory: ${{initial-clone}}/apartments-api
    # dockerfile: apartments-api/Dockerfile
    image-name: dorbel/apartments-api
    tag: develop
  unit-test:
    image: ${{build-the-image}}
    working-directory: /home/nodejs/app
    commands:
        - npm test
